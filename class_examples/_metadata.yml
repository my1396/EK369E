# Shared metadata for the class_example/ directory
resources:
  - "../filters/emoji/*.png"

# Pandoc Extensions
from: markdown+tex_math_single_backslash
filters:
  - ../filters/color-text.lua
  - ../filters/unlisted-sections.lua
format:
  pdf:
    include-in-header: ../latex/preamble.tex
    pdf-engine: xelatex
    fontsize: 12pt
   #  keep-tex: true
